# Agents

Read [CLAUDE.md](CLAUDE.md) — it is the conventions entry point for every
coding agent working in this repo, whatever the tool. It holds the
always-relevant rules and routes to the deep reference skills in
`.claude/skills/` (`component-api`, `styling-reference`,
`sanity-and-preview`, `seo-discoverability`, `setup`), which are plain
markdown any assistant can follow.
